language: java

sudo: false
  
matrix:
  include:
    - os: linux
      jdk: oraclejdk8

script:
    - mvn starts:starts 
    - ls -la
    - ls -a
    
cache:
    directories:
    - $HOME/.m2
    - .starts
